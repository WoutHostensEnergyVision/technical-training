<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- list View -->
        <record id="view_bakker_koeken_tags_list" model="ir.ui.view">
            <field name="name">bakker.koeken.tags.list</field>
            <field name="model">bakker_koeken_tags</field>
            <field name="arch" type="xml">
                <list>
                    <field name="name"/>
                    <field name="color" widget="color_picker"/>
                </list>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_bakker_koeken_tags_form" model="ir.ui.view">
            <field name="name">bakker.koeken.tags.form</field>
            <field name="model">bakker_koeken_tags</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="color" widget="color_picker"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action -->
        <record id="action_bakker_koeken_tags" model="ir.actions.act_window">
            <field name="name">Koeken Tags</field>
            <field name="res_model">bakker_koeken_tags</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Menu Item -->
        <menuitem id="menu_bakker_koeken_tags"
                  name="Koeken Tags"
                  action="action_bakker_koeken_tags"
                  sequence="10"/>
    </data>
</odoo>